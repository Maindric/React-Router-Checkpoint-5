class Profile {
    constructor(firstName, lastName, birthday, profileImageUrl) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.birthday = birthday;
        this.profileImageUrl = profileImageUrl;   
    }
}

export default Profile;